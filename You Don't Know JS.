#You Don't Know JS
###第一章：作用域
程序中源代码在执行前，会经历三个阶段：词法分析（字符串分解为有意义的代码块），语法分析（将代码块转化为代表程序语法的树），代码生成。

引擎：负责js编译和执行；编译器：负责词法分析及代码生成；作用域：确定代码对标识符的访问权限。举个例子： var a = 2;

1 遇到var a，编译器询问作用域是否有变量a，如果有则忽略声明，如果没有则当前作用域声明一个新变量，并命名为a。
2 遇到a=2,引擎运行时会首先询问作用域，当前的作用域集合中是否存在一个叫a的变量，如果是，引擎会使用这个变量，
如果否，引擎会继续查找该变量。如果引擎最终找到了a则将2赋值给他，否则会抛出一个异常。

LHS查询：                                                              
RHS查询：
